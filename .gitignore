# ============================================================
#  运行时凭证与本地状态 —— 含 Token / 会话凭证，绝不可推送到公开仓库
#  注意：Git 的 .gitignore 不支持行内注释（# 只在行首才是注释），
#        因此下面每一行必须是「纯模式」，注释单独成行。
# ============================================================

# 你的实际配置（内含 PushPlus / Server酱 Token）
config.json

# 凭证缓存（内含 Trae refreshToken 与 userId）
state.json

# 需人工处理的信号文件
NEED_ACTION.txt

# ============================================================
#  运行日志
# ============================================================
logs/
*.log

# ============================================================
#  本地调试与探测脚本（开发用，不进仓库）
# ============================================================
_probe/
*.manual-bak-*
*.checkin-bak

# ============================================================
#  系统 / 编辑器
# ============================================================
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# ============================================================
#  依赖目录（本项目零第三方依赖，此处为防御性排除）
# ============================================================
node_modules/
