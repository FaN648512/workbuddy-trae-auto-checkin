# 项目优化日志

> **项目**：`workbuddy-trae-auto-checkin` —— WorkBuddy + Trae 双平台每日自动签到助手
> **记录日期**：2026-09-14
> **记录范围**：从立项调研 → 核心实现 → 三轮加固 → 发布准备的全过程

---

## 目录

- [一、同类优秀项目调研](#一同类优秀项目调研)
  - [1.1 调研对象与选样理由](#11-调研对象与选样理由)
  - [1.2 核心优势 / 特色亮点 / 设计风格对照](#12-核心优势--特色亮点--设计风格对照)
  - [1.3 提炼出的五条可借鉴实践](#13-提炼出的五条可借鉴实践)
  - [1.4 本项目的差异化定位](#14-本项目的差异化定位)
- [二、优化日志（逐项）](#二优化日志逐项)
  - [A 组 · 可用性基础](#a-组--可用性基础)
  - [B 组 · 鉴权与凭证链路](#b-组--鉴权与凭证链路)
  - [C 组 · 健壮性与自愈](#c-组--健壮性与自愈)
  - [D 组 · 可观测性与可运维](#d-组--可观测性与可运维)
  - [E 组 · 通知](#e-组--通知)
  - [F 组 · 发布准备](#f-组--发布准备)
- [三、成果汇总](#三成果汇总)

---

## 一、同类优秀项目调研

### 1.1 调研对象与选样理由

用 WebSearch 检索「WorkBuddy / Trae 自动签到」品类的开源实现，按**技术路线差异**取样，
覆盖「模拟点击派」「直连接口派」「云端派」三条路线：

| # | 项目 | 技术路线 | 选样理由 |
|---|---|---|---|
| 1 | `codeLong1024/workbuddy-checkin` | Python 标准库，直连接口 | 品类内 README 最专业，模块化设计思路可借鉴 |
| 2 | `lbh1nb/workbuddy-auto-sign` | Node + Playwright，模拟点击 | 代表「不碰接口、只点按钮」的保守路线 |
| 3 | `star620/TRAE-Checkin` | C# + .NET 9 + WebView2，带 GUI | 唯一有完整图形界面的实现，可看交互设计 |
| 4 | `Maquer/trae-signin` | 纯 Go 标准库 + GitHub Actions | 代表「云端定时、不依赖本机挂机」路线 |
| 5 | `88lin/workbuddy-auto-signin` | Python + AI 自动化触发 | 明确对比「AI 触发 vs 系统静默」两种模式的成本 |

### 1.2 核心优势 / 特色亮点 / 设计风格对照

| 维度 | 标杆做法 | 观察与判断 |
|---|---|---|
| **核心优势** | `workbuddy-checkin`：零依赖 + 零凭证落盘 + 可插拔模块<br>`auto-sign`：双击即用，零终端操作 | 品类的共同痛点是**「要不要相信一个能读你登录态的脚本」**——所有优秀实现都把「安全透明」当成第一卖点，而不是功能数量 |
| **特色亮点** | `trae-signin`：多账号批量 + 自动刷新过期 token<br>`TRAE-Checkin`：云端 GitHub Actions + OAuth 一键部署<br>`auto-signin`：模式 A/B 成本对比 | 差异化集中体现在**「凭证生命周期管理」**上——谁能把续期做扎实，谁就更接近「装上就不用管」 |
| **设计风格（视觉）** | `TRAE-Checkin`：仪表盘 + 设置页 + 云端状态卡片，主色调克制<br>`workbuddy-checkin`：纯文字 README，用表格 + 引用块 + 分级清单组织信息<br>多数项目：**无徽章、无目录、无截图** | 品类内 README 普遍**结构薄弱**（大段连续文字、缺导航）。用标准信息架构（徽章/目录/表格/FAQ）就能显著拉开差距 |
| **排版手法** | `workbuddy-checkin` 的分级引用块免责声明、模块对照表、`~` 路径约定 | 「表格化 + 可扫读」是这类小工具 README 的最优解 |
| **共性缺口** | ① 几乎都只做**单平台**<br>② 凭证过期普遍**只报错不自动恢复**<br>③ 无差异化**失败告警**（失败也不通知，或每天都通知）<br>④ 缺少**自诊断**手段 | 这四点正是本项目的发力方向 |

### 1.3 提炼出的五条可借鉴实践

1. **安全必须前置，且可被验证。** 把「不存凭证、只读不改、零第三方依赖」写在最显眼处，
   并给出可核对的具体行为（读哪个文件、只读什么）。
2. **信息架构优先于措辞。** 用「表格 + 分级清单 + 引用块」组织，比写长段落有效得多；
   长文档一律配可点击目录。
3. **明确写出「它做不到什么」。** 品类的可信度来自边界清晰：接口归属、可能失效、
   与官方无隶属关系，全部显式声明。
4. **失败路径要设计，不能只设计成功路径。** 优秀的实现都说明了「失败时你看到什么、该怎么办」。
5. **降低首跑门槛。** 最短路径控制在 3 步内，并给出「预期输出」让人确认真的跑通了。

### 1.4 本项目的差异化定位

> **一句话定位**：一个把「装上就不用管」做到位的双平台签到助手 —— 凭证过期自己续、
> 续完自己回写、真失败才叫你。

| 差异点 | 同类项目普遍做法 | 本项目 |
|---|---|---|
| **平台覆盖** | 只做 WorkBuddy **或** 只做 Trae | **一个脚本同时管两个平台**，共用一套重试/通知/日志骨架 |
| **凭证过期** | 报错，让你手动重登 | **自动用 refreshToken 续期**（Trae 已实测打通） |
| **续期副作用** | 只换不写，客户端下次启动掉登录 | **续期后加密回写登录态文件**（往返加密结果字节一致，实测无损） |
| **失败告警** | 不通知，或每天都通知 | **条件化推送**：仅「签到未全部成功 **且** WorkBuddy 处于登录状态」才推 |
| **故障排查** | 让你翻源码猜 | **`--diagnose` 一条命令**打印每个候选路径的错误码 |
| **运行形态** | 需常驻守护进程 / 需挂机 | **Windows 计划任务** 三次触发，脚本幂等，不常驻 |

---

## 二、优化日志（逐项）

每条按 **动机 → 改动 → 作用 → 预期效果** 四段记录。

---

### A 组 · 可用性基础

#### A1 · 确定「自建」而非「套用」的技术路线

- **动机**：GitHub 上现成实现不少，但逐个试读后发现每家的登录态识别、接口参数都各有一处坑，
  直接套用会把这些坑一并继承，且难以定位问题。
- **改动**：只借鉴**算法与接口定义**（Trae 解密算法、claim 接口参数要求），
  主脚本完全自建，逻辑内聚在一个文件里。
- **作用**：问题定位链条变短——出问题只需读一个文件；不必跟随上游仓库的更新节奏。
- **预期效果**：后续任一平台接口变更，改一处即可修复，不依赖第三方维护。

#### A2 · 零第三方依赖

- **动机**：签到工具要能长期无人值守运行，任何第三方依赖都可能成为未来的故障点
  （版本冲突、依赖被删、安装失败）。
- **改动**：只用 Node 内置模块（`crypto` / `fs` / `path` / `https` / `child_process`），
  单文件可运行，无需 `npm install`。
- **作用**：部署成本降为零；不受依赖生态变动影响；审计面小（代码全在自己手里）。
- **预期效果**：换机器时拷两个文件（`checkin.js` + `run_checkin.cmd`）即可跑，不会有「装了但跑不起来」。

#### A3 · Windows 计划任务调度

- **动机**：最可靠的方式是让操作系统来调度，而不是常驻一个守护进程或依赖 AI 自动化
  （后者每次触发会消耗模型调用，成本可能高于签到收益）。
- **改动**：注册计划任务 `DailyCheckin_WorkBuddy_Trae`，三个触发器：
  **每天 07:30** + **每天 20:30** + **登录 Windows 后延迟 3 分钟**。
- **作用**：不常驻进程、不消耗 AI 积分；开机时间不固定也能保证当天签上。
- **预期效果**：只要「开机 + 登录进桌面 + 联网」，当天必然签到一次，无需任何手动操作。

#### A4 · 幂等设计

- **动机**：多触发器意味着一天会运行多次，若不做幂等会造成重复请求，有触发风控的风险。
- **改动**：以平台返回的「今日已签到」为成功态，重复运行只复查并快速退出。
- **作用**：多次触发安全；补跑逻辑可以放心叠加。
- **预期效果**：一天触发 N 次 = 一天签到 1 次，重复运行零副作用。

---

### B 组 · 鉴权与凭证链路

#### B1 · 攻克 Trae 登录态解密

- **动机**：Trae CN 的登录态存的是密文（`iCubeAuthInfo://icube.cloudide`），
  不还原出明文 token 就无法调用任何接口。
- **改动**：移植并验证解密算法——
  取 Base64 密文的 `[6:38]` 为随机数、`[38:]` 为密文主体；
  用 `SHA512(SHA512(随机数) ‖ (SALT_A ⊕ SALT_B))` 派生 **AES-128-CBC** 的 Key/IV；
  解出后去掉 64 字节头部即得 JSON。
- **作用**：打通整条链路的前提；本机实测成功取出 JWT 与 refreshToken。
- **预期效果**：Trae 侧无需任何手动登录操作即可完成签到。

#### B2 · 修复 claim 接口 `9004`（字段级坑）

- **动机**：领取接口首次调用返回 **HTTP 200** 但业务码 `9004`
  「The submitted order parameters are incorrect」——只判 HTTP 状态码会被误判为成功。
- **改动**：定位到根因是缺少 **`x-device-id`** 请求头（必须是 16 位纯数字设备号），补齐该头。
- **作用**：从「看起来成功实际没领到」变为真正领取成功。
- **预期效果**：Trae 200 积分（150 基础 + 50 额外）正常到账。

#### B3 · 设备 ID 提取方式改良

- **动机**：同类项目多通过解析应用日志文件提取设备号，受日志轮转与格式变更影响，脆弱。
- **改动**：发现设备号直接编码在 `storage.json` 的键名里
  （`iCubeAuthInfo://icube-dc:<16位数字>:`），改为从键名正则提取，日志解析仅作兜底。
- **作用**：取值稳定，不依赖日志文件是否还在。
- **预期效果**：设备号提取成功率不受日志清理影响。

#### B4 · Trae refreshToken 自动续期

- **动机**：原实现遇到凭证过期只会报错，需要人工重新登录，违背「装上就不用管」的目标。
- **改动**：加入 L2 层逻辑——当 accessToken 剩余有效期低于阈值（默认 60 分钟）
  或已过期时，自动用 refreshToken 换取新凭证。
- **作用**：凭证过期从「需要人工介入的故障」降级为「自动处理的常规事件」。
- **预期效果**：Trae 凭证长期保持有效，用户无感。

#### B5 · 续期后加密回写登录态文件（关键差异点）

- **动机**：实测发现 **Trae 的 refreshToken 会轮换**（续期后旧值失效）。
  若只把新凭证存在工具内部、不回写平台文件，**Trae 客户端下次启动会要求重新登录**——
  这是很多现成脚本没有处理的隐患。
- **改动**：用与解密对称的算法重新加密新凭证，原子写回 `storage.json`；
  写前自动备份 `storage.json.checkin-bak`；提供 `--no-writeback` 开关可关闭。
- **作用**：工具续期与客户端登录态保持一致，不会「工具能签到、客户端却掉登录」。
- **预期效果**：**加密往返实测通过**——重新加密后密文 2464 字节，与原值完全一致；
  顶层 22 个键、设备 ID 键、account 字段全部无损。

#### B6 · WorkBuddy 改走「客户端自刷新」

- **动机**：从 `app.asar`（297 MB）里挖出了应用自身的刷新接口
  （`POST /v2/auth/token/refresh`，带 `X-Refresh-Token` / `X-Auth-Refresh-Source: plugin` / `X-Domain` 头），
  但在 `copilot.tencent.com`、`www.codebuddy.cn`、`www.workbuddy.cn`、`api.codebuddy.cn`
  等域名下试了 7 种路径前缀，**全部 404 Route Not Found**——该接口跑在专有网关上，公网不可达。
- **改动**：放弃猜域名，改用**唯一 100% 可靠的手段**——
  凭证快过期时自动拉起 `WorkBuddy.exe`，由客户端自身完成登录态刷新，随后轮询读取新凭证。
  同时加入**限频保护**（一天最多 2 次、间隔 ≥3 小时），避免凭证彻底失效时反复弹窗。
- **作用**：在不依赖未公开接口的前提下解决续期问题；限频避免打扰。
- **预期效果**：WorkBuddy 凭证（accessToken 60 天 / refreshToken 90 天）长期有效；
  两个月内正常开过一次客户端就不会过期。

---

### C 组 · 健壮性与自愈

#### C1 · 分层自愈架构（L1–L7）

- **动机**：签到失败的原因很多（凭证过期、限流、网络抖动、路径变更），
  若用一串 `if` 平铺处理，会互相干扰且难以扩展。
- **改动**：整理为七层递进的自愈策略：

  | 层 | 触发条件 | 动作 |
  |---|---|---|
  | L1 | 凭证有效 | 直接签到 |
  | L2 | 剩余 <60 分钟或已过期 | 用 refreshToken 自动换新 |
  | L3 | 续期成功 | 回写登录态文件，客户端不掉线 |
  | L4 | 网络抖动 / 5xx / 429 | 指数退避重试 |
  | L5 | Trae `9074` 限流 | 3 次温和重试（15~25 秒随机） |
  | L6 | 无法自动恢复 | 落 `NEED_ACTION.txt` + 推送通知 + 退出码 2 |
  | L7 | 登录文件路径变更 | 自动扫描候选目录重新发现 |

- **作用**：故障处理有明确层级，每层职责单一、可单独调试。
- **预期效果**：绝大多数异常在 L2–L5 内自动消化，用户只会感知到「签到了」或「收到一条真失败通知」。

#### C2 · Trae 服务器限流重试

- **动机**：Trae 高峰期返回 `9074`（参与用户过多），属临时性限流，立即放弃会白丢一天积分。
- **改动**：识别 `9074` 后温和重试 3 次，间隔 15~25 秒**随机**（避免所有脚本同时重试加剧拥堵）。
- **作用**：把「高峰期失败」转为「稍后成功」。
- **预期效果**：签到成功率显著提升，且不给服务端造成额外压力。

#### C3 · 网络层指数退避

- **动机**：短时网络抖动（DNS、连接重置）在家庭网络下并不少见。
- **改动**：网络错误重试 3 次，指数退避（基础 2.5 秒）。
- **作用**：抗瞬时网络故障。
- **预期效果**：偶发网络问题不再导致签到失败。

#### C4 · 登录文件读取重试（修复「误报警」）

- **动机**：实测发现 WorkBuddy 登录文件会**间歇性读不到**——
  日志里 14:22:37 报「未找到登录文件」，12 秒后的 14:22:49 完全正常。
  后果严重：一次瞬时读不到就会走「凭证失效」分支，**推送一条假的「签到失败」到微信**。
  排查过程：12 秒内采样 254 次 `existsSync` → **0 次丢失**，排除权限与沙箱因素；
  判断是客户端**原子改写文件时的短暂窗口**（8 次运行里撞上 2 次）。
- **改动**：WorkBuddy 与 Trae 两侧的登录文件读取统一改为**重试 4 次、间隔 400ms**；
  正常情况首次命中、零等待，撞上抖动则自动恢复并在日志留记号：
  `↩ 第 N 次尝试才读到…（瞬时抖动，已自动恢复）`。
- **作用**：消除误报根源。
- **预期效果**：验证方式为隔离复现——把脚本复制到临时目录、
  用环境变量把 `LOCALAPPDATA`/`APPDATA` 指向假目录，再让后台进程在第 900ms 才创建登录文件，
  结果如实打印 `↩ 第 4 次尝试才读到` 并成功恢复。**测试目录用后即删，未触碰真实凭证。**

#### C5 · 显式进程退出

- **动机**：Node 的全局 keep-alive socket 会吊住进程，表现为计划任务永远显示「运行中」
  （`LastTaskResult = 267009`），看起来像卡死。
- **改动**：脚本结尾按结果显式 `process.exit(code)`。
- **作用**：进程确定性退出。
- **预期效果**：计划任务状态正确回到 `Ready`，实测脚本 1~5 秒跑完。

#### C6 · 日志逐行实时落盘

- **动机**：原实现把日志缓存在内存、结束时一次性写盘。
  进程若被强制终止（超时、断电、系统结束进程），**缓冲整批丢失**，
  事后完全看不出跑到哪一步——这个坑实际撞上过一次（日志零增长，看起来像任务失败）。
- **改动**：改为**逐行实时落盘**。
- **作用**：进程异常终止时最多只丢最后一行。
- **预期效果**：任何异常都能留下可追溯的痕迹。

#### C7 · 日志日期用本地时区（隐藏 bug）

- **动机**：日志文件名原用 `toISOString()` 取日期——那是 **UTC**。
  北京时间 8 点前 UTC 仍停在前一天，导致**07:30 那班的日志被写进「昨天」的文件**，
  早上查今天的日志会看到空白，误以为没跑。
- **改动**：改用本地日期函数生成文件名。
- **作用**：日志归属日期与人的直觉一致。
- **预期效果**：早上查当天日志能看到当天的记录。

---

### D 组 · 可观测性与可运维

#### D1 · 语义化退出码

- **动机**：计划任务与外部脚本需要程序化判断结果，仅有文字输出不够。
- **改动**：`0` = 全部成功；`1` = 可重试失败（网络/接口）；`2` = 需人工介入（需重新登录）。
- **作用**：让上层调度与告警能区分「等等会好」和「得动手了」。
- **预期效果**：可无缝接入任何自动化编排。

#### D2 · `NEED_ACTION.txt` 信号文件

- **动机**：需要人工处理的状态应当有**持久化痕迹**，不应只存在于一次性日志里。
- **改动**：无法自动恢复时写入 `NEED_ACTION.txt`（含原因与处理建议），恢复成功后自动清除。
- **作用**：给非技术使用者一个明确的「出事了看这个文件」入口。
- **预期效果**：存在与否即可一眼判断健康状态。

#### D3 · `--diagnose` 环境诊断

- **动机**：原来的失败信息只有一句「未找到登录文件」，无法定位是路径不对、无权限还是被占用。
- **改动**：新增诊断命令，逐条打印每个候选路径及其错误码
  （`ENOENT` 不存在 / `EACCES` 无权限 / `EBUSY` 被占用）、环境变量是否齐全、
  以及含重试的最终结论。
- **作用**：把「猜原因」变成「读结论」。
- **预期效果**：3 秒看清全貌，日志直接给出原因。

#### D4 · `--self-test` 离线自检

- **动机**：排障时需要区分「代码坏了」和「网络/凭证坏了」。
- **改动**：新增 10 项离线自检（路径探测、加解密往返、有效期解析等），**不发任何网络请求**。
- **作用**：可在断网状态下验证代码完整性。
- **预期效果**：自检通过 ⇒ 问题必在环境或凭证侧，排查范围大幅收窄。

#### D5 · 终端输出设计

- **动机**：这是无 GUI 的 CLI 工具，「终端输出」就是它唯一的界面，需要一眼可读。
- **改动**：统一输出规范——
  **状态符号**（`✅` 成功 / `❌` 失败 / `↩` 自动恢复 / `📣` 已通知 / `🔄` 续期中 / `🚀` 拉起客户端）；
  **键值对齐**（`账号：xxx    设备ID：xxx`）；
  **结论优先**，细节收进 `--verbose`；**任何情况下不打印 token 内容**。
- **作用**：输出既适合人读（一眼看懂结果），也适合机器读（稳定格式）。
- **预期效果**：即使不看日志文件，瞄一眼终端就知道结果与是否需要处理。

---

### E 组 · 通知

#### E1 · 五通道可插拔通知

- **动机**：不同人习惯的推送渠道不同，硬编码单一渠道会限制可用性。
- **改动**：抽象出统一通知层，支持 **PushPlus（微信）/ Server酱（微信）/ 企业微信群机器人 / 飞书 / Bark（iOS）**，
  全部通过 `config.json` 切换，留空即静默关闭。
- **作用**：通知渠道与签到逻辑解耦。
- **预期效果**：改渠道只需改一行配置，无需动代码。

#### E2 · 条件化推送（避免误报打扰）

- **动机**：失败的推送需要有信噪比——如果「人不在电脑前导致失败」也推送，
  会让人对通知麻木；若完全不推送，真失败又无从知晓。
- **改动**：推送条件设为 **「签到未全部成功」且「WorkBuddy 处于登录状态」**。
  含义是：**能读出 WorkBuddy 凭证 = 电脑开着、环境正常，此时失败是真异常，值得打扰**；
  反之则视为正常现象，不推送。同时 `--status` 手动查询模式不会触发通知，避免调试时误发。
- **作用**：只推有意义的失败。
- **预期效果**：通知可信度高，收到即可直接去处理。

#### E3 · 响应体业务码校验（修复「假成功」）

- **动机**：PushPlus 用 **HTTP 200** 返回业务错误——
  实测假 token 时服务器返回 `HTTP 200 / {"code":903,"msg":"用户令牌不正确"}`。
  若只判 HTTP 状态码，会把**发送失败当成发送成功**，用户永远等不到消息却以为一切正常。
- **改动**：在通知层加入响应体业务码校验（PushPlus / Server酱 / 企业微信均有此类行为）。
- **作用**：消除静默失败。
- **预期效果**：平台侧异常在日志中被明确标出，实测能正确报出 `用户令牌不正确`。

---

### F 组 · 发布准备

#### F1 · `.gitignore` 密钥隔离

- **动机**：本项目要发布为**公开仓库**，而 `config.json` 含 PushPlus Token、
  `state.json` 含 Trae refreshToken 与 userId——一旦提交即等同于公开泄露。
- **改动**：建立 `.gitignore`，排除 `config.json`、`state.json`、`NEED_ACTION.txt`、
  `logs/`、`_probe/`、各类备份文件与编辑器目录。
- **作用**：从机制上杜绝密钥进仓库，而不是靠人记得删。
- **预期效果**：任何一次误 `git add -A` 都不会把凭证推上去。

#### F2 · `config.example.json` 模板

- **动机**：使用者需要知道配置长什么样，但不能看到真实密钥。
- **改动**：提供脱敏模板，`token` 留空、`type` 默认为 `none`（即默认不推送），
  并保留每个字段的中文说明。
- **作用**：开箱即用的配置起点，同时演示「默认关闭通知」的安全默认值。
- **预期效果**：新用户复制模板 → 填自己的值 → 即可运行，不需要读源码。

#### F3 · MIT 许可证

- **动机**：没有明确许可证的代码在法律上不可被他人使用，会阻碍传播与贡献。
- **改动**：添加标准 MIT `LICENSE`。
- **作用**：许可清晰，允许使用、修改、分发。
- **预期效果**：他人可放心借鉴与二次开发。

---

## 三、成果汇总

| 维度 | 落地成果 |
|---|---|
| **平台覆盖** | WorkBuddy + Trae（TRAE SOLO CN / Trae CN 等 6 个候选目录名） |
| **依赖** | 零第三方依赖（仅 Node 内置模块） |
| **调度** | Windows 计划任务，07:30 + 20:30 + 登录后 3 分钟 |
| **凭证** | Trae 自动续期 + 加密回写（往返字节一致）；WorkBuddy 客户端自刷新 + 限频 |
| **自愈** | L1–L7 七层递进，覆盖限流 / 网络 / 路径变更 / 瞬时读写抖动 |
| **通知** | 5 通道可插拔，条件化推送（真失败才推） |
| **可运维** | `--self-test` / `--status` / `--diagnose` / `--verbose` / `--notify-test` / `--simulate-failure` / `--no-writeback` |
| **实测** | 双平台签到成功；Trae 200 积分到账；计划任务 `LastTaskResult = 0` |
| **安全** | 密钥隔离 + 只读平台登录态 + token 不打印 / 不外传 / 不落盘 |
